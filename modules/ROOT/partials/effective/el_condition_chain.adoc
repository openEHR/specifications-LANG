[cols="2,3", frame=none, grid=rows]
|===
2+^h|*EL_CONDITION_CHAIN*

2+a|Compound expression consisting of a chain of condition-gated expressions, and an ungated `_else_` member that as a whole, represents an if/then/elseif/else chains.

Evaluated by iterating through `_items_` and for each one, evaluating its `_condition_`, which if True, causes the evaluation result of the chain to be that item's `_result_` evaluation result.

If no member of `_items_` has a True-returning `_condition_`, the evaluation result is the result of evaluating the `_else_` expression.

2+|Inherits: xref:{class_el_expression}[EL_EXPRESSION], xref:{class_el_decision_table}[EL_DECISION_TABLE]

2+h|*Attributes*

|*else*: `xref:{class_el_expression}[EL_EXPRESSION]` [1..1]
a|Result expression of the chain if no member of `_items_` succeeds in evaluation. +
_Inherited from xref:{class_el_decision_table}[EL_DECISION_TABLE]_

|*items*: `link:/classes/List[List] <xref:{class_el_conditional_expression}[EL_CONDITIONAL_EXPRESSION]>` [1..1]
a|Members of the chain, equivalent to branches in an if/then/else chain and cases in a case statement.

2+h|*Functions*

|_(abstract)_ *eval_type* (): `xref:{class_bmm_type}[BMM_TYPE]` [1..1]
a|Meta-type of expression entity used in type-checking and evaluation. 

Effected in descendants. +
_Inherited from xref:{class_el_expression}[EL_EXPRESSION]_

|*is_boolean* (): `link:/classes/Boolean[Boolean] ` +
 +
__Post_result__: `Result = eval_type().equal( \{BMM_MODEL}.boolean_type_definition())` [1..1]
a|True if `_eval_type_` is notionally Boolean (i.e. a `BMM_SIMPLE_TYPE` with `_type_name()_` = `Boolean`). +
_Inherited from xref:{class_el_expression}[EL_EXPRESSION]_
|===
