[cols="2,3", frame=none, grid=rows]
|===
2+^h|*BMM_ASSERTION*

2+a|A statement that asserts the truth of its expression. If not, generates an exception (depending on run-time settings).

May be rendered in syntax as `assert condition` or similar.

2+|Inherits: xref:{class_bmm_routine_body}[BMM_ROUTINE_BODY], xref:{class_bmm_statement_item}[BMM_STATEMENT_ITEM], xref:{class_bmm_statement}[BMM_STATEMENT], xref:{class_bmm_simple_statement}[BMM_SIMPLE_STATEMENT]

2+h|*Attributes*

|*expression*: `xref:{class_el_boolean_expression}[EL_BOOLEAN_EXPRESSION]` [1..1]
a|

|*tag*: `link:/classes/String[String] ` [0..1]
a|Optional tag, typically used to designate design intention of the assertion, e.g. `"Inv_all_members_valid"`.
|===
